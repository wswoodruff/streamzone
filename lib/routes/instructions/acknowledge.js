'use strict';
const Joi = require('@hapi/joi'); const id = require('../../validation/ids'); const Response = require('../../http/management-response');
module.exports = { method: 'POST', path: '/streamers/{streamerId}/instructions/{versionId}/warning-acknowledgement', options: { auth: 'session', validate: { params: Joi.object({ streamerId: id.required(), versionId: id.required() }) } }, handler: async (request, h) => { try { return await request.services().instructionService.acknowledgeWarnings(request.auth.credentials.id, request.params.streamerId, request.params.versionId); } catch (error) { return Response.fromError(error, h); } } };
