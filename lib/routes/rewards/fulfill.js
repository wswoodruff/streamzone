'use strict';
const Joi = require('@hapi/joi'); const id = require('../../validation/ids'); const Response = require('../../http/management-response');
module.exports = { method: 'POST', path: '/streamers/{streamerId}/redemptions/{redemptionId}/fulfill', options: { auth: 'session', validate: { params: Joi.object({ streamerId: id.required(), redemptionId: id.required() }) } }, handler: async (request, h) => { try { return await request.services().rewardService.fulfillManually(request.auth.credentials.id, request.params.streamerId, request.params.redemptionId); } catch (error) { return Response.fromError(error, h); } } };
