'use strict';
module.exports = { method: 'POST', path: '/streamers/{streamerId}/provider-connections/{connectionId}/youtube/synchronize', options: { auth: 'session' }, handler: async (request, h) => { try { await request.services().youtubeProviderService.synchronize(request.auth.credentials.id, Number(request.params.streamerId), Number(request.params.connectionId)); return h.redirect(`/dashboard/stream-management?streamerId=${request.params.streamerId}&notice=${encodeURIComponent('YouTube broadcasts synchronized.')}#broadcasts`); } catch (error) { return h.redirect(`/dashboard/stream-management?streamerId=${request.params.streamerId}&error=${encodeURIComponent(error.message)}#broadcasts`); } } };
