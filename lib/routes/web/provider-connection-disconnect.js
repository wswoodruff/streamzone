'use strict';
module.exports = { method: 'POST', path: '/streamers/{streamerId}/provider-connections/{connectionId}/disconnect', options: { auth: 'session' }, handler: async (request, h) => { try { await request.services().providerOAuthService.revoke(request.auth.credentials.id, Number(request.params.streamerId), Number(request.params.connectionId)); return h.redirect(`/dashboard/stream-management?streamerId=${request.params.streamerId}&notice=${encodeURIComponent('YouTube disconnected.')}#sources`); } catch (error) { return h.redirect(`/dashboard/stream-management?streamerId=${request.params.streamerId}&error=${encodeURIComponent(error.message)}#sources`); } } };
